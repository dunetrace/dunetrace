# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
*.whl
dist/
build/
extracted/
.eggs/
.Python
pip-log.txt
pip-delete-this-directory.txt
.installed.cfg

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment variables — never commit secrets
.env
.env.local
.env.*.local

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
nosetests.xml
coverage.xml
*.cover

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
CLAUDE.md
.claude/
.claude.json
# Cursor MCP config holds a local API key; commit .cursor/mcp.json.example instead
.cursor/mcp.json
# Stray CLAUDE.md that was committed as bare ".md" (root only)
/.md

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Logs
*.log
logs/

# Docker
*.env.docker

# Database
*.sqlite3
*.db

# Deployment — not in use yet
railway.toml

# SSH / TLS / secrets — never commit private keys or certs
/config
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
node_modules/
**/node_modules/

# npm
*.tgz
.npm/

# Local scratch notes — not part of the tracked repo
BACKLOG.md

# Pre-production audit working files — internal diligence output, not source
audit/

# Local docker overrides (often contain local ports/secrets) & merge artifacts
docker-compose.override.yml
docker-compose.override.yaml
*.orig
*.rej

# Marketing drafts
docs/blog/

# Docs copied into the MCP server package at build time (see
# packages/mcp-server/setup.py). Generated from docs/ — never edit or commit;
# docs/ is the single source of truth.
packages/mcp-server/dunetrace_mcp/_docs/
