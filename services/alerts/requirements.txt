asyncpg==0.29.0
httpx==0.28.1
python-dotenv>=1.2.2
PyYAML>=6.0
# audit Finding 4 (BLOCKER): alerts_svc/crypto.py imports cryptography to decrypt
# per-org Slack/Linear creds, but it was missing here — clean builds crash-looped
# with ModuleNotFoundError. Matches services/api/requirements.txt.
cryptography>=42
# Required by dunetrace_schemas (apply_migrations -> dunetrace_schemas/__init__ -> events.py).
# This service does not use pydantic directly, but importing the shared migration
# runner pulls the package __init__, so the image cannot boot without it.
pydantic==2.10.4
